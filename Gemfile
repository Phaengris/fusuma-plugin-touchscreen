# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

# gem "fusuma", "~> 2.5"
gem 'fusuma', path: '/home/debian/git/fusuma'

gem "memoized", "~> 1.1"

gem "rspec", "~> 3.12"
